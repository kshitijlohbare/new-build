// This is a simple direct export file to ensure proper importing of FocusTimer
import FocusTimerComponent from './FocusTimer';

// Re-export with a console log for debugging
console.log('FocusTimer index bridge loaded successfully');

// Export the component as default
export default FocusTimerComponent;
